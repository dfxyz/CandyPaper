<!DOCTYPE html>
<html lang="zh">
<head>
  <title>Color Table</title>
  <meta charset="UTF-8">
  <style>
    p {
        font-family: 'Fira Code', sans-serif;
      margin: 0;
      padding: 0.5em;
    }
  </style>
</head>
<body>
<h1>Preview FG and BG</h1>
<%_ for (const outerItem of groupByBg ? bgColorItems : fgColorItems) { _%>
  <%_ for (const innerItem of groupByBg ? fgColorItems : bgColorItems) { _%>
    <%_ const fgColor = groupByBg ? innerItem.color : outerItem.color; _%>
    <%_ const fgAliases = groupByBg ? innerItem.aliases : outerItem.aliases; _%>
    <%_ const bgColor = groupByBg ? outerItem.color : innerItem.color; _%>
    <%_ const bgAliases = groupByBg ? outerItem.aliases : innerItem.aliases; _%>
    <div style="color: #<%= fgColor.hex %>; background-color: #<%= bgColor.hex %>">
      <p>FG: #<%= fgColor.hex%>, HSV(<%= fgColor.hsv %>), RGB: (<%= fgColor.rgb %>) Aliases=[<%= fgAliases %>] </p>
      <p>BG: #<%= bgColor.hex%>, HSV(<%= bgColor.hsv %>), RGB: (<%= bgColor.rgb %>) Aliases=[<%= bgAliases %>] </p>
      <p>Contrast Ratio: <%= fgColor.contrastRatio(bgColor) %></p>
    </div>
  <%_ } _%>
<%_ } _%>
</body>
</html>
